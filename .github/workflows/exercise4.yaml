name: exercise-4
on:
    pull_request:
        branches:
            - dev
jobs:
    exercise4:
        runs-on: ubuntu-latest
        steps:
            - name: message
              run: "echo Thanks for pull request!"