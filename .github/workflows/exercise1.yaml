---
   name: exercise1
   on:
    push:
        branches:
            - main

   jobs:
      learning:
        runs-on: ubuntu-latest
        steps:
            - name: learning
              run: "echo Hello How are You?"
            - name: list env
              run: printenv